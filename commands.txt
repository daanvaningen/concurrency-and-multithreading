# compile
./bin/build.sh

# run (accept-cycle file, 1_naive version, 1 core)
./bin/ndfs.sh input/accept-cycle.prom 1_naive 1


Notes:

Zowel de naive versie als de improved versie vind niks bij bench-wide.sumo
Is de file te groot? duurt het gewoon lang?

Improved versie:
Geen deadlock in: Accept-cycle.prom, simple-loop.prom, bench-deep.sumo
Deadlock in: bintree-cycle-min.prom 
